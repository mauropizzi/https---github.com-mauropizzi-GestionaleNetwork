// src/pages/ClientiList.tsx
// No direct code change needed here, as the ImportResult type has been updated.
// Ensure you are importing and using the `ImportResult` type correctly.
// Example:
// import { ImportResult } from '@/types'; // Or wherever you defined it
// ...
// const result: ImportResult = await importCsv(...);
// if (result.duplicateRecords.length > 0) { ... }